#pragma warning(disable : 4996)  

#include <stdio.h>
#include <string.h>

int main()
{
	int T = 0;

	scanf("%d", &T);

	for (int i = 0; i < T; i++)
	{
		int A, B, N;
		scanf("%d %d %d", &A, &B, &N);


	}

	for (int i = 0; i < T; i++)
	{
		printf("Case #%d: %d\n", i + 1, i);
	}

	return 0;
}